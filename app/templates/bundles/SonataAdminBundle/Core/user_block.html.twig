{% if app.user %}
    <span class="email">
        {{ app.user.email }}
    </span>
    |
    <a href="{{ path('logout') }}">
        <i class="fa fa-sign-out"></i> Logout
    </a>
{% else %}
    <a href="{{ path('login') }}">
        <i class="fa fa-sign-in"></i> Login
    </a>
{% endif %}